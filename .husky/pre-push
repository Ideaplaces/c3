echo "[pre-push] Type checking src/..."
if npx tsc --noEmit 2>&1 | grep -q "^src/"; then
  npx tsc --noEmit 2>&1 | grep "^src/"
  echo "[pre-push] Type errors in src/, aborting push."
  exit 1
fi
echo "[pre-push] OK"
